defmodule APIWeb.User.ProfileController do
  use APIWeb, :controller

  plug API.Plug.Authorization

  def show(conn, _) do

  end

  def create(conn, _) do

  end

  def update(conn, _) do

  end

end
