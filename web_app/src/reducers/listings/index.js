const initialState = {
  page: {
    current: 1,
    tota: 1
  },
  properties: []
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state
    }
  }
}

export default reducer